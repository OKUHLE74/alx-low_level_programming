#include <stdio.h>

/**
 * main - prints its name
 * @argc: - argurment count
 * @argv: - argurment vector
 *
 * Return: Always zero
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
